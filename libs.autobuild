# IMPORTANT: new packages must be added at the top of this file
# They must NEVER be added to other files

bundle_package 'bundles/eds'
cmake_package 'slam/eds'
import_package 'tools/torch' do |pkg|
    def pkg.build
    end

    def pkg.install
        super
        in_dir (srcdir) do
            run("build", "wget https://download.ifi.uzh.ch/rpg/eds/patches/torch.patch -O CMakeLists.txt")
        end
    end

end

cmake_package 'perception/depthnn'

